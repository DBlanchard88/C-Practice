#include <stdio.h>
#include <stdlib.h>

int main()
{
    
    printf("%d", 70);

    return 0;
}

/* can also use command such as "const int num = 5"
a constant is a variable that can not change. see print f example */
